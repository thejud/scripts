#!/usr/bin/env ruby

require 'date'

if ARGV.empty?
  puts DateTime.now.to_time.to_i
  exit 0
end

s = ARGV.join(" ")
if /\D/.match(s)
  puts DateTime.parse(s).to_time.to_i
else
  if s.size > 12
    t = s.to_i/1000.0  # milliseconds
  else
    t = s.to_i
  end
  puts Time.at(t)
end
